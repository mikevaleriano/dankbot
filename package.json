{
  "name": "dankbot",
  "type": "module",
  "description": "This motherfucker...",
  "author": "Mike Valeriano <mkvlrn@gmail.com>",
  "version": "0.1.1",
  "license": "MIT",
  "engines": {
    "node": "14.8"
  },
  "scripts": {
    "dev": "nodemon --exec \"node --loader ts-node/esm --experimental-specifier-resolution=node -r dotenv/config src/bot\"",
    "build": "rimraf build && tsc",
    "start": "node --experimental-specifier-resolution=node build/bot",
    "lint": "eslint src/**/*.ts --fix && tsc --noemit"
  },
  "dependencies": {
    "axios": "^0.20.0",
    "discord.js": "^12.3.1"
  },
  "devDependencies": {
    "@types/node": "^14.6.3",
    "@types/ws": "^7.2.6",
    "dotenv": "^8.2.0",
    "eslint-config-mkvlrn": "^6.0.3",
    "nodemon": "^2.0.4",
    "rimraf": "^3.0.2",
    "ts-node": "^9.0.0",
    "typescript": "^4.0.2"
  }
}
